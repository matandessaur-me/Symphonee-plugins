{
  "version": 1,
  "updated": "2026-04-13",
  "learnings": [
    {
      "category": "cli-flags",
      "cli": "claude",
      "summary": "claude: CLI \"claude\" rejected flags [\"--effort\",\"low\",\"--model\",\"haiku\",\"-p\",\"--no-input\"]: error: unknown option '--no-input'.",
      "detail": "Failed args: [\"--effort\",\"low\",\"--model\",\"haiku\",\"-p\",\"--no-input\"]",
      "addedAt": "2026-04-05"
    },
    {
      "category": "cli-flags",
      "cli": "claude",
      "summary": "claude: CLI \"claude\" rejected flags [\"--model\",\"haiku\",\"-p\",\"--no-input\"]: error: unknown option '--no-input'. Update HEADLESS_F",
      "detail": "Failed args: [\"--model\",\"haiku\",\"-p\",\"--no-input\"]",
      "addedAt": "2026-04-05"
    },
    {
      "category": "orchestration",
      "cli": "claude",
      "summary": "When testing two branches side by side with worktrees, dispatch two parallel agents (one per branch) instead of running everything sequentially in one thread. Each agent can set up its worktree, install deps, start its dev server, and run tests independently.",
      "detail": null,
      "addedAt": "2026-04-07"
    },
    {
      "category": "orchestration",
      "cli": "claude",
      "summary": "When research or parallel work is needed, ALWAYS use the DevOps Pilot orchestrator (POST /api/orchestrator/spawn) to dispatch worker agents (Gemini, Codex, Grok, etc.) instead of using built-in subagent/Agent tools. The orchestrator is the correct dispatch mechanism; built-in agents bypass it and the user cannot see the work in the orchestrator UI.",
      "detail": null,
      "addedAt": "2026-04-07"
    },
    {
      "category": "general",
      "cli": null,
      "summary": "Run-Query.ps1 in DevOps Pilot takes -File (path to a .ps1 file), not -Query (WIQL string). It is a script runner, not a WIQL executor. Write WIQL logic into a .ps1 in .ai-workspace first, then pass the path via -File.",
      "detail": "Error: A parameter cannot be found that matches parameter name Query. The script just does & $File after validating the file exists.",
      "addedAt": "2026-04-09"
    },
    {
      "category": "api-pattern",
      "cli": null,
      "summary": "Before calling an endpoint, verify it exists in the documented API reference. Guessed routes return 404 and waste a round-trip. When a field you want to filter on is not supported as a query parameter, pull what is available and filter the rest in memory.",
      "detail": null,
      "addedAt": "2026-04-09"
    },
    {
      "category": "general",
      "cli": null,
      "summary": "When merging an array with existing data read from an API, always normalize the existing value first. If the source uses an unusual separator or raw string, naive concatenation can produce a single mashed-together entry instead of separate ones. Split on the expected delimiter with a regex that tolerates whitespace, then join back with a canonical separator.",
      "detail": null,
      "addedAt": "2026-04-09"
    },
    {
      "category": "api-pattern",
      "cli": null,
      "summary": "WIQL does not allow ORDER BY on the Tags field. Queries with ORDER BY [System.Tags] fail with a 400 Bad Request. Order by Id, State, or WorkItemType instead. The error body is often empty, which makes this hard to diagnose.",
      "detail": null,
      "addedAt": "2026-04-09"
    },
    {
      "category": "api-pattern",
      "cli": null,
      "summary": "sendTrace and sendException have different signatures. sendTrace is (message, severityLevel=1, properties={}) -- 3 args. sendException is (exceptionType, message, method, properties={}) -- 4 args. Do not use the 4-arg exception shape for sendTrace.",
      "detail": null,
      "addedAt": "2026-04-09"
    },
    {
      "category": "api-pattern",
      "cli": "claude",
      "summary": "The xlsx (SheetJS) library strips all formatting (colors, borders, fonts, column widths, merged cells) when you convert a sheet to an array with sheet_to_json and rebuild with aoa_to_sheet. To preserve styles when modifying an Excel file, manipulate cells in-place on the existing worksheet object: use shiftRowsDown to move cells, then write new cell values directly with writeRow copying style from a reference row. Read with { cellStyles: true } and write with { cellStyles: true }.",
      "detail": "sheet_to_json -> modify array -> aoa_to_sheet destroys all Excel formatting. Instead: (1) read existing cell addresses, (2) shift rows down from insertion point, (3) write new values into individual cells, copying .s (style) from a nearby reference row. This preserves the original workbook formatting.",
      "addedAt": "2026-04-10"
    },
    {
      "category": "api-pattern",
      "cli": "claude",
      "summary": "The SheetJS (xlsx) npm package community edition does NOT preserve styles, colors, or formatting -- even with cellStyles:true. That is a Pro-only feature. Use exceljs instead, which reads and writes xlsx files with full style preservation (fills, fonts, borders, tab colors, merged cells, row heights, column widths). When modifying existing rows, unmerge any merged ranges first before clearing/rewriting cells, otherwise merged cell values bleed into new data.",
      "detail": "Previous learning about shiftRowsDown + cellStyles is wrong -- SheetJS community cannot do it. exceljs is the correct library. Key gotcha: if the original file has merged cells (e.g. A44:F44 for a TOTAL row), you must call ws.unMergeCells(range) BEFORE clearing and rewriting those rows, otherwise the merge persists and corrupts new data written to those cells.",
      "addedAt": "2026-04-10"
    },
    {
      "category": "general",
      "cli": "claude",
      "summary": "In markdown, a blank line inside a pipe table breaks the table. All table rows must be contiguous with no empty lines between them. When inserting new rows into an existing markdown table, never leave a blank line between the old last row and the new first row, or the new rows will render as raw pipe-delimited text instead of table cells.",
      "detail": null,
      "addedAt": "2026-04-10"
    },
    {
      "category": "general",
      "cli": "claude",
      "summary": "When extracting Elementor sections from a page to use as templates, always verify the actual column count and structure rather than relying on design doc descriptions. The design notes may describe an idealized layout (e.g. 3-col [20%,60%,20%]) while the actual implementation uses a different structure (e.g. 1-col 100%). Always inspect the JSON data before coding against it.",
      "detail": null,
      "addedAt": "2026-04-10"
    },
    {
      "category": "general",
      "cli": "claude",
      "summary": "Elementor section background_color set via API does not render on the frontend until the CSS cache is regenerated (open in Elementor editor + click Update). For immediate rendering without cache regeneration, use the CSS viewport breakout technique on the HTML widget itself: wrap content in an outer div with width:100vw; position:relative; left:50%; transform:translateX(-50%); background:#color, and cap inner content with max-width:1440px; margin:0 auto. Also clear _elementor_css meta to force cache regeneration on next load.",
      "detail": null,
      "addedAt": "2026-04-10"
    },
    {
      "category": "general",
      "cli": "claude",
      "summary": "When using the CSS 100vw breakout technique (width:100vw; left:50%; transform:translateX(-50%)) inside Elementor HTML widgets, always add: (1) overflow-x:clip on the breakout div to prevent horizontal scrollbar from scrollbar-width mismatch, (2) max-width:100vw as fallback, (3) overflow:hidden on the parent Elementor section via custom_css, (4) zero out all padding on the Elementor column via custom_css (selector > .elementor-element-populated { padding: 0 !important; }). Without these, mobile views get horizontal overflow and inward padding gaps.",
      "detail": null,
      "addedAt": "2026-04-10"
    },
    {
      "category": "general",
      "cli": "claude",
      "summary": "Elementor responsive widget settings (align_mobile, _margin_mobile, etc.) written via API do NOT take effect until the CSS cache is regenerated by opening the page in Elementor editor and clicking Update. For immediate mobile fixes without editor access, use custom_css on the section with @media queries and the selector keyword. Custom CSS is embedded inline and renders immediately. This is the only reliable way to fix mobile layout via API.",
      "detail": null,
      "addedAt": "2026-04-10"
    },
    {
      "category": "shell",
      "cli": "claude",
      "summary": "Push-AndPR.ps1 breaks when -Description contains commas, apostrophes, or other special characters. PowerShell string parsing fails with TerminatorExpectedAtEndOfString. Instead, push the branch manually with git push -u origin HEAD, then create the PR via curl to POST /api/pull-request with a JSON body, which handles special characters correctly.",
      "detail": null,
      "addedAt": "2026-04-10"
    },
    {
      "category": "api-pattern",
      "cli": null,
      "summary": "Builder-style image audits can produce false positives when custom components render alt text from fields such as imageAltText or imageAltTag instead of alt or altText.",
      "detail": "Before bulk editing CMS image alt text, inspect the consuming component code and treat component-specific alt fields as valid. Do not rely only on a generic audit that recognizes alt and altText.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "platform",
      "cli": null,
      "summary": "System.Drawing Bitmap.Save may throw a generic GDI+ error when saving generated images into dot-prefixed workspace folders.",
      "detail": "If this happens, save the generated image to a normal writable path, verify the file exists, then clean it up after use. Do not assume the drawing code failed before testing a simpler output path.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "shell",
      "cli": null,
      "summary": "In PowerShell loops, Invoke-RestMethod failures can leave a previous response variable in place and make later logging look like a false success.",
      "detail": "Reset the response variable before each request or wrap Invoke-RestMethod in try/catch. On catch, log the failure separately and do not read stale response data from a previous iteration.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "shell",
      "cli": null,
      "summary": "curl.exe JSON POSTs from PowerShell can hang or time out when inline body quoting is misescaped.",
      "detail": "For local API writes from PowerShell, prefer Invoke-RestMethod with a hashtable converted by ConvertTo-Json, or write complex JSON to a temp file and post that. Avoid deeply escaped inline curl JSON bodies.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "platform",
      "cli": null,
      "summary": "Node fs.writeFile can hit EPERM when writing generated files into a dot-prefixed workspace folder.",
      "detail": "If Node cannot write a generated manifest or temporary output into a dot-prefixed workspace folder, emit the data to stdout or write to another verified writable path, then clean up. Do not keep retrying the same path.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "shell",
      "cli": "node",
      "summary": "When parsing markdown table rows with node -e, always guard cells.length before indexing. Header separator rows (|---|---|) split into fewer cells than data rows and cause TypeError on cells[N].replace. Skip rows where cells.length is below the expected column count.",
      "detail": "Encountered TypeError: Cannot read properties of undefined (reading replace) while parsing a markdown table via l.split(|). The offending line was a style-laden STORY row that split unevenly due to embedded HTML. Fix: add `if (cells.length < EXPECTED) continue;` before destructuring, and strip HTML with replace(/<[^>]+>/g, ) before trimming.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "api-pattern",
      "cli": null,
      "summary": "Builder.io plugin GET /entries/:model hangs/404s; use GET /content/:model?limit=N instead. /entries is not the real route.",
      "detail": "The instructions table lists Get-Entries.ps1 (-Model), but the REST route for listing entries is /api/plugins/builderio/content/MODEL_NAME?limit=N. Calling /api/plugins/builderio/entries/MODEL never returns.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "api-pattern",
      "cli": null,
      "summary": "Builder.io /insights?issue=missing-alt under-reports: default detector only matches alt/altText siblings. Custom field names (imageAltTag, videoAltTag, desktopImageAltTag, featuredImage without alt sibling, etc.) are missed and counts come back as 0 even when hundreds of images lack alt.",
      "detail": "For accurate alt audits on spaces with custom component schemas, fetch /content/MODEL and walk the data tree yourself. Recognize: alt, altText, altTag, imageAlt*, desktopImageAlt*, mobileImageAlt*, videoAlt*, iconAlt*, featuredImageAlt, mediaAlt*. Also handle LocalizedValue objects where each locale key may be a string URL or alt text.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "shell",
      "cli": null,
      "summary": "curl -o to a path whose parent dir does not exist fails with exit code 23 and size=0 even though it prints HTTP:200. The HTTP status is returned before write, so it is misleading.",
      "detail": "On Windows bash, shell cwd can reset between tool calls, landing in a subdir. Any relative path that assumed the project root then fails. Always mkdir -p the target dir in the same bash call as the curl, or use an absolute path. Always check curl exit code, not just the HTTP status from -w.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "api-pattern",
      "cli": null,
      "summary": "Builder.io plugin does not expose GET /assets/:id for single-asset fetch. Calling it hangs until the bash timeout. Use GET /assets?query=... or /assets?limit=... and filter by id in the response instead.",
      "detail": "The routes module registers PATCH /assets/:id and DELETE /assets/:id but no GET. An unmatched GET path falls through to a handler that never responds, so curl hits its timeout rather than getting a 404.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "api-pattern",
      "cli": null,
      "summary": "Builder.io plugin /assets?query=<value> can time out at 30s because it paginates through the full asset library on the Builder admin API for each call. Use a larger curl --max-time (120s+) or pre-cache via /assets full scan first.",
      "detail": "Asset search is not a real server-side filter on the Builder side, the plugin scans pages and filters locally. Short timeouts will kill the request mid-scan.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "general",
      "cli": null,
      "summary": "Before bulk-writing alt text to a Builder.io space, verify the model schema defines an alt sibling subfield. If not, the frontend component may be reading alt from a specific custom key and a PATCH that writes altText will be silently ignored by the rendered page.",
      "detail": "A Builder.io model can have a seoFields object with only metaTitle, metaDescription, featuredImage, schemaJson subfields. The Builder UI still shows a per-usage 'Alt text for this page' input, but that writes an ad hoc field the frontend may or may not render. Always map image field to alt field by inspecting the consuming component source before bulk PATCHing the Write API.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "api-pattern",
      "cli": null,
      "summary": "Builder.io Write API enforces Firestore document limits: PATCH returns 500 with 'Input object is deeper than 20 levels or contains a cycle' when the merged data tree exceeds 20 nesting levels. Cannot patch deeply nested page entries (often Symbol-within-Symbol layouts). Workaround: edit those entries via Builder.io UI which can flatten on save, or refactor the page structure first.",
      "detail": "Hit on a Pricing page with many Columns -> Sections -> Symbols nesting. The PATCH body itself is small, but the resulting merged document violates Firestore's 20-level depth limit.",
      "addedAt": "2026-04-13"
    },
    {
      "category": "general",
      "cli": null,
      "summary": "When deduping CDN image URLs by their first 12 hex chars of the asset id, you can collide. Different uploads of the same source photo share a long common prefix. Always dedupe by full URL (or full asset id), not a truncated prefix.",
      "detail": "In a bulk image audit, two different Builder.io assets started with the same 12 hex chars (9b5322c33a0d) but referenced different files. The truncated-prefix dedup map silently dropped one of them and that entry got NO-OP'd.",
      "addedAt": "2026-04-13"
    }
  ]
}
